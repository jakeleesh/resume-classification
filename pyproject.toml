[project]
name = "resume-classification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openpyxl>=3.1.5",
    "pandas>=3.0.0",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
